import styles from "../../styles/components/LoadMoreButton.module.scss";

export const LoadMoreButton: React.FC = () => {
  return (
    <div className={styles.container}>
      <span>Carregar mais</span>
    </div>
  );
};
